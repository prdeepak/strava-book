# Docker Compose for isolated workspace: ws-d15b80
# Generated by workspace-manager.sh - do not edit directly

services:
  web:
    build: ./web
    container_name: strava-ws-ws-d15b80
    working_dir: /app/web
    volumes:
      - .:/app
      - ws-d15b80_node_modules:/app/web/node_modules
      - ${HOME}/.gcp:/app/.gcp:ro
      - ${HOME}/.aws:/root/.aws:ro
    ports:
      - "3001:3000"
    env_file:
      - ./web/.env.local
    environment:
      - WORKSPACE_ID=ws-d15b80
    command: sh -c "npm install --legacy-peer-deps && npm run dev"

volumes:
  ws-d15b80_node_modules:
