# Docker Compose for isolated workspace: ws-a12100
# Generated by workspace-manager.sh - do not edit directly

services:
  web:
    build: ./web
    container_name: strava-ws-ws-a12100
    working_dir: /app/web
    volumes:
      - .:/app
      - ws-a12100_node_modules:/app/web/node_modules
      - ${HOME}/.gcp:/app/.gcp:ro
      - ${HOME}/.aws:/root/.aws:ro
    ports:
      - "3001:3000"
    env_file:
      - ./web/.env.local
    environment:
      - WORKSPACE_ID=ws-a12100
    command: sh -c "npm install --legacy-peer-deps && npm run dev"

volumes:
  ws-a12100_node_modules:
