Commit and push changes to git:

1. Run `git status` and `git diff --stat` to see all changes
2. Analyze the changes and propose a concise commit message
3. **STOP and wait for user to confirm or edit the message**
4. Once confirmed, run: `make sync msg="<confirmed message>"`
